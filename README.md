# 수원숲정신건강의학과 홈페이지

정적 파일만으로 동작하는 병원 홈페이지입니다. 빌드 도구나 서버 없이 `index.html`을 열면 바로 실행됩니다.

## 구조

```
index.html              홈페이지 (원페이지)
assets/css/styles.css   디자인 토큰 · 라이트/다크 테마 · 반응형
assets/js/screenings.js 자가진단 척도 데이터 + 채점 로직
assets/js/main.js       테마·네비게이션·자가진단 진행 UI
app/                    기존 '불안 관리 앱' (공황 버튼 · 명상 · 산책 · 감정 일기)
```

## 섹션

히어로 → 이런 마음이라면 → 진료 안내 → 무료 자가진단 → 의료진 소개 →
진료시간·오시는 길 → 셀프케어 앱 → 자주 묻는 질문 → 예약 CTA → 푸터

## 자가진단

임상에서 널리 쓰이는 공개 선별 척도 5종이 실제로 동작합니다.

| 검사 | 척도 | 문항 | 채점 |
|---|---|---|---|
| 우울 | PHQ-9 | 9 | 총점 0–27 |
| 불안 | GAD-7 | 7 | 총점 0–21 |
| 불면증 | ISI | 7 | 총점 0–28 |
| 성인 ADHD | ASRS-v1.1 Part A | 6 | 문항별 기준 초과 개수 (4개 이상 시 시사) |
| 스트레스 | PSS-10 | 10 | 총점 0–40 (4·5·7·8번 역채점) |

- 응답과 결과는 `localStorage`(`suwonsup.screening.v1`)에만 저장되며 서버로 전송되지 않습니다.
- 결과 화면의 '기록 삭제' 버튼으로 언제든 지울 수 있습니다.
- PHQ-9 9번(자해·죽음에 대한 생각)에 응답하면 위기상담 안내가 함께 표시됩니다.
- 선별(screening) 참고용이며 의학적 진단이 아니라는 안내를 카드·결과·푸터에 모두 표기했습니다.

## 반영된 병원 정보

- 상호: 수원숲 정신건강의학과의원 (Suwon Forest Mental Health Clinic)
- 대표번호: 031-548-1903
- 주소: 경기도 수원시 팔달구 효원로 279, 205호 (인계동, 농협 경기본부 건물 2층)
- 지하철: 수인분당선 수원시청역 9·10번 출구, 도보 1~2분
- 주차: 건물 내 지하 주차장, 진료 시 1시간 무료
- 진료시간: 월·수·금 09:30–18:30 / 화·목 09:30–19:30 / 토 09:00–14:00 /
  점심 13:00–14:00 / 일요일·공휴일 휴진
- 방문 전 체크리스트: 신분증(필수), 기존 약 처방전(권장), 시간적 여유(권장)

로고는 원본 로고(열린 원 + 나무)를 SVG로 옮겨 `index.html` 안의
`<symbol id="logo-mark">` 에 정의했고, 헤더·푸터·약도·파비콘이 이를 공유합니다.
색상은 `currentColor` 를 따르므로 다크 모드에서 자동으로 밝은 초록으로 바뀝니다.

## 의료진

원장 정재균 (정신건강의학과 전문의) — 학력·수련과 주요 경력을 `#doctor` 섹션에 반영했습니다.

## 이미지 (선택)

`assets/img/` 에 파일을 넣으면 자동으로 적용되고, 없으면 코드로 그린 도형이
대신 표시됩니다. 자세한 규격은 [`assets/img/README.md`](assets/img/README.md) 참고.

| 파일 | 쓰이는 곳 |
|---|---|
| `assets/img/logo-mark.png` | 헤더·푸터 로고 (마크만, 배경 투명) |
| `assets/img/doctor.jpg` | 의료진 소개 사진 |

`build-single.js` 가 이 이미지들을 data URI 로 `dist/index.html` 안에 심어 넣으므로,
파일 하나만 올려도 로고와 사진이 함께 표시됩니다.

## 아직 확인·교체가 필요한 부분

- **원장 소개 문구** — `#doctor` 의 `.doctor-quote` 는 예시로 작성한 문장입니다.
  원장님 본인의 말로 바꾸거나 삭제하세요.
- 사업자등록번호·대표자 등 푸터 표기 항목 (현재는 표시하지 않음)
- 약도(`.minimap`)는 직접 그린 간이 약도입니다. 실제 지도를 쓰려면
  네이버/카카오 지도 iframe으로 교체하세요.

## 파일 하나로 배포하기

```
node build-single.js
```

`dist/` 폴더가 만들어집니다. CSS·JS가 모두 `dist/index.html` 안에 들어 있어
이 폴더를 웹호스팅(Netlify Drop 등)에 그대로 올리면 됩니다.

## 커스터마이즈

- **색상**: `assets/css/styles.css` 상단 `:root` / `[data-theme="dark"]` 변수만 바꾸면 전체 톤이 바뀝니다.
- **검사 추가/수정**: `assets/js/screenings.js`의 `SCREENINGS` 배열에 항목을 추가하면 카드와 진행 화면이 자동 생성됩니다.
  문항별로 보기가 다르면 문자열 대신 `{ text, options }` 객체를 넣으면 됩니다.
